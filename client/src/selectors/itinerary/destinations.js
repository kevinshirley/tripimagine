import R from 'ramda';
import { createStructuredSelector, createSelector } from 'reselect';

// export const selectCurrentRegionCode = R.path(['site', 'region', 'code']);

export default createStructuredSelector({

});